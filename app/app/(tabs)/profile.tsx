import { ThemeView, ThemeText } from "@/components";

export default function Tab() {
  return (
    <ThemeView className="items-center justify-center">
      <ThemeText>Tab [Home|Settings]</ThemeText>
    </ThemeView>
  );
}
